import renderer from 'react-test-renderer';
import Timer from '../Timer';

// test('Timer renders correctly', () => {
//   const tree = renderer.create(<Timer endTime={new Date()} />).toJSON();
//   expect(tree).toMatchSnapshot();
// });